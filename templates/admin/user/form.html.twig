{% extends 'admin/adminbase.html.twig'%}
{% block body %}
    <div class="container">
        <div class="row no-gutters">
            <div class="col-12">
                <h1 class="display-4 text-center mt-5">
                    {{ title }}
                </h1>
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}